// Vite 中替代 Webpack require.context + requireAll 的完整写法
// 1. 匹配 ./svg 目录下所有 .svg 文件（不递归子目录），eager: true 同步加载（和原逻辑一致）
const svgModules = import.meta.glob('./svg/*.svg', { eager: true });

// 2. 还原 requireAll 函数（保留 map 遍历逻辑，和原代码一致）
const requireAll = (modules) => {
  // 遍历文件路径数组，返回模块内容数组（对应原 map(requireContext)）
  return Object.keys(modules).map((filePath) => modules[filePath]);
};

// 3. 执行遍历（等价于原 requireAll(req)）
const svgModuleList = requireAll(svgModules);
